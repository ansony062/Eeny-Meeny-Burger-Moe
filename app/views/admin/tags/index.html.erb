<div class="container">
  <div class="row">
    <h3>タグ一覧・追加</h3>
    <div class="col-8">
      <%= render 'layouts/errors', obj: @tag %>
      <%= render 'new', tag: @tag %>
    </div>

    <div class="col-8">
      <table class="table table-hover">
        <thread>
          <tr>
            <th>タグ</th>
            <th></th>
          </tr>
        </thread>
        <tbody>
          <% @tags.each do |tag| %>
            <tr>
              <td><%= tag.name %></td>
              <td><%= link_to "編集する", edit_admin_tag_path(tag), class: "btn btn-primary" %></td>
            <tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
