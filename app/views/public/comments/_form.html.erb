<%= form_with(model:[post, comment], local: false) do |f| %>
  <%= f.text_area :post_comment, rows: '3', placeholder: "コメントをここに記入", size: "40px" %>
  <%= f.submit "送信", class: "btn btn-primary btn-sm m-3" %>
<% end %>