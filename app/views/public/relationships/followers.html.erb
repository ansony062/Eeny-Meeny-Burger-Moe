<div class="container">
  <h3>Follower Users</h3>
  <div class="row">
    <div class="col-8 mx-auto">
      <% if @users.exists? %>
        <%= render "public/users/index", users: @users %>
      <% else %>
        <p>ユーザーはいません。</p>
      <% end %>
    </div>
  </div>
</div>
